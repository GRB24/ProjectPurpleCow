import { Typography } from "@mui/material";

export const Header = () => {
    return (
        <header>
            <Typography variant='h1'>
                Project Purple Cow
            </Typography>
        </header>
    )
}